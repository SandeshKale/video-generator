{
  "name": "reel-app",
  "version": "0.1.0",
  "private": true,
  "description": "POC: React/Vite reel authoring app. Built once with `vite build`, then rendered to MP4 by ../scripts/render.mjs using the same window.__seek(t) frame-stepping contract as the plain-HTML reels.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
